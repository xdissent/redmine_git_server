<%= form_for(@post_receive_hook) do |f| %>
  <% if @post_receive_hook.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post_receive_hook.errors.count, "error") %> prohibited this post_receive_hook from being saved:</h2>

      <ul>
      <% @post_receive_hook.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :url %><br />
    <%= f.text_field :url %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
