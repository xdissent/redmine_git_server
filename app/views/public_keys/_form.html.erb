<%= form_for(@public_key) do |f| %>
  <% if @public_key.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@public_key.errors.count, "error") %> prohibited this public_key from being saved:</h2>

      <ul>
      <% @public_key.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :raw_content, l(:label_public_key_raw_content) %><br />
    <%= f.text_area :raw_content %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>